// Datepicker stuff for forms
import datepicker from 'js-datepicker';

let dateField = document.querySelector('#event-date');
if(dateField) {
  const picker = datepicker('#event-date');
}

import Tagify from '@yaireo/tagify';
